# This file simply maps a simple dataset name to the data facets needed to load knn ground truth
# You can use either relative or absolute paths here
# my_test_data:
#  base: path_to_base_vectors.fvecs
#  query: path_query_vectors.fvecs
#  gt: path_to_ground_truth_indices.ivecs
#  # optional, but overrides the base path/url to the file if given:
#  baseurl: s3://my-bucket/datapath
